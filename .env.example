# PostgreSQL connection string (provided by Render PostgreSQL or any Postgres host)
DATABASE_URL=postgresql://user:password@host:5432/htmeetings

# Secret used to sign session cookies — generate a long random string
SESSION_SECRET=change-me-to-a-long-random-string

# Port the server listens on (Render sets PORT automatically)
PORT=3000
